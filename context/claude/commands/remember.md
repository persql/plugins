---
description: Save a durable fact to shared PerSQL context
---

Use the `persql-context` MCP `remember` tool to store this durable fact so
other agents and future sessions see it: $ARGUMENTS

Write it self-contained, with the keywords someone would later search for. If
it replaces an existing fact, pass that fact's id in `supersedes`.
